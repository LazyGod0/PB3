import { Box, Typography } from "@mui/material";

function Logpop(){
    return(
        <Box sx={{
            display: "flex",
            justifyContent: "center",
            alignItems: "center",
            height: "100vh",
            backgroundColor: "#f0f0f0"
        }}>
            <Typography variant="h1" sx={{ color: "#16325B", fontWeight: "bold" }}>
                poppppp
            </Typography>
        </Box>
    );
}

export default Logpop;
